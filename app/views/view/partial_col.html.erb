<div id="list">

  <%= render partial: 'books/book', collection: @books,
    locals: { type: :details }, spacer_template: 'separator', layout: 'books/frame'%>

  <%# collectionオプションの省略形 %> 
  <%#= render @books, type: :details %>



<%# collectionオプションを利用しない場合 %>
<%# @books.each do |book| %>
  <%#= render 'books/book', book: book, type: :details %>
<%# end %>

</div>