<!DOCTYPE html>
<html>
  <head>
    <title><%= @title || 'Railbook' %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

<body>
　　<div class="d-flex flex-column flex-md-row align-items-center p-2 px-md-10 bg-white border-bottom shadow-sm">
　　　　<%= link_to "図書リスト", books_path, method: :get, class: "my-0 mr-md-auto font-weight-normal Editlink h2" %>
　　　　<nav class="my-2 my-md-0 mr-md-3">
 <%
=begin%>
 　　　　　　<a class="p-2 text-dark" href="#">Features</a> 
 <%
=end%>

  <br />
  <div class="search-form">
    <%= form_with url: search_path, method: :get, local: true do |f| %>
      <%= f.text_field :keyword, value: @keyword %>
      <%= f.submit "検索" %>
    <% end %>
    </div>

　　　　</nav>
      <% if logged_in? %>
        <%= render 'users/logged_in' %>
        <%= link_to "ログアウト", logout_path, method: :delete %>
      <% else %>
        <%= link_to 'ログイン/登録', new_user_path, class: "btn bg-primary text-white" %>
      <% end %>
　　</div id="">
    <div class="contents-wrap">
　　<%= content_for?(:content) ? yield(:content) : yield %> 
    </div>
    <footer class="footer">
      <div class="container">
        <p class="text-muted"></p>
      </div>
    </footer>
</body>
</html>